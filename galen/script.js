const form=document.getElementById('chatForm');const input=document.getElementById('messageInput');const messages=document.getElementById('messages');const clearBtn=document.getElementById('clearChat');const replies=['Понял. Давай разберем это по шагам.','Могу помочь с архитектурой, UI или логикой продукта.','Для Galenite я бы сделал это более минималистично.'];function addMessage(text,type){const wrap=document.createElement('div');wrap.className='msg';const avatar=document.createElement('b');avatar.textContent=type==='bot'?'G':'Y';const bubble=document.createElement('p');bubble.textContent=text;wrap.appendChild(avatar);wrap.appendChild(bubble);messages.appendChild(wrap);messages.scrollTop=messages.scrollHeight}input?.addEventListener('input',()=>{input.style.height='auto';input.style.height=Math.min(input.scrollHeight,160)+'px'});form?.addEventListener('submit',e=>{e.preventDefault();const value=input.value.trim();if(!value)return;addMessage(value,'user');input.value='';input.style.height='60px';setTimeout(()=>{addMessage(replies[Math.floor(Math.random()*replies.length)],'bot')},700)});clearBtn?.addEventListener('click',()=>{messages.innerHTML=''})